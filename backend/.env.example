# Backend configuration
# REQUIRED
JWT_SECRET=your_change_me_jwt_secret

# The allowed CORS origin for the frontend (e.g. http://localhost:5173)
# NOTE: set this to the exact scheme+host+port you serve the frontend from.
CORS_ORIGIN=http://localhost:5173

# Optional
PORT=8000

