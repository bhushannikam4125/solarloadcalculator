# Frontend configuration
# Where the backend API is hosted (used by axios).
# For docker-compose production with Nginx+reverse proxy, consider using http://backend:8000
VITE_API_BASE_URL=http://localhost:8000

